# Pedidos e insights de 2026-09-24 — registro e decisões

Registro de **tudo** o que foi pedido na sessão de 2026-09-24, para nada se perder: cada pedido, cada link, onde ficou registrado, a decisão tomada e o estado.

Premissa da sessão: **não existe mais BiaML**. Tudo é PINNeAPPle e vive nas pastas de `pinneapple-labs`. O inventário de métodos foi refeito só com código daqui.

---

## 1. Pedidos

| # | Pedido | Decisão | Estado |
|---|---|---|---|
| 1 | Visualização 3D de nível industrial para gêmeos digitais | Pacote `pinneapple_twin3d` na lib (ver D1) | Feito: exportação glTF + campos transientes + sensores com envelope e WebSocket; visualizador web; 4 testes |
| 2 | Inventário de solvers, métodos de treino, equações físicas e problemas físicos | Refeito só com código de `pinneapple-labs` | Feito (na conversa); vira código no pedido 4 |
| 3 | Validar o inventário; excluir o que não está validado e trazer de volta só com paper + benchmark | Critério objetivo, sem apagar código (ver D5) | Feito: `docs/dev/CATALOGO_METODOS.md` (53 validados, 58 com teste sem referência, 64 sem teste) |
| 4 | Referência bibliográfica para **todo** método, validado ou novo | Catálogo `pinneapple_catalog.methods`, com referência e estado de validação por item | Feito: arXiv conferidos; corrigida citação errada do X-TFC (arXiv:2005.01219 → 1812.08625) |
| 5 | Pesquisar papers de SciML para implementar soluções com os benchmarks dos papers e pôr as equações na lib | Começa pelo SSBFGS/SSBroyden (item L1), que traz 5 benchmarks do próprio paper | Feito o otimizador (T20) + benchmark Helmholtz; faltam Euler/HLLC, Burgers invíscida, Stokes, PK-PD |
| 6 | Extrair dos links o que pode ser implementado ou melhorado | Seção 2 abaixo | Feito (análise); implementação por item |
| 7 | Integrar datasets públicos de geometria/malha e de Physics AI | Catálogo em código `pinneapple_catalog.resources` (ver D3, D4) | Feito: 58 recursos, checagem de licença, `fetch` do HF testado |
| 8 | Integrar o mapa de modelos pré-treinados | Mesmo catálogo | Feito |
| 9 | Integrar PressNet, PLAID, AB-UPT e GeoPT | Mesmo catálogo. O AB-UPT já tem bridge só-pesquisa | Feito |
| 10 | "Tome as decisões a partir da estrutura do pinneapple-labs" | Seção 3 abaixo | Feito |
| 11 | A loss do `PINNFactory` quebrava com `Tamb(t)`; torná-la flexível para aceitar e entender parâmetros assim | Sinais externos detectados das equações, constantes e constantes de execução; bug silencioso `S(t)` → `t` corrigido | Feito: 8 testes, inclusive identificação inversa de `a` e `b` |
| 12 | Testar o pipeline completo do notebook Open-Meteo e garantir que funciona | `examples/pinn_solver/06_open_meteo_climate_twin.py`: A (factory) e B (loss manual) reproduzem o notebook; C é o modelo corrigido | Feito. A e B rodam e dão os mesmos números, mas não aprendem a física (MAE 1,78 °C, `a` < 0, `b` não identificável porque Tamb = T). C (solo × ar, S ≥ 0, `a`,`b` > 0 via exp, Fourier diário, SSBroyden): offline recupera a = 40,5 / b = 55,9 (verdadeiros 40 / 60); com dados reais de Natal, a = 90,1, b = 63,5 (τ = 2,6 h), MAE 0,53 °C, what-if +30% sol = +0,26 °C na média |
| 13 | Subir as alterações e atualizar o PyPI | Branch + PR a partir de worktree limpo (há outra sessão no mesmo checkout); versão 0.6.0 | Feito: PR #14 mergeado, tag `v0.6.0`, https://pypi.org/project/pinneapple/0.6.0/ (testado instalando a wheel num ambiente novo) |

### Continuação (2026-09-25): itens 1 a 12 da lista "o que ainda falta"

| # | Item | Estado |
|---|---|---|
| 1 | Benchmarks dos papers do SSBFGS/SSBroyden (Helmholtz completo; Euler/HLLC, Burgers invíscida, Stokes, PK-PD) | Harness `benchmarks/ssqn_paper_benchmarks.py` com referências independentes (Sod exato confere com Toro; Burgers por características; PK-PD por Radau). Stokes do upstream é Gauss-Newton, fora. PK-PD rodado: os 4 otimizadores falham com o PINN simples (rel-L2 ≈ 0,99; o SSBroyden tem a menor loss). Burgers, Sod e Helmholtz ainda rodando |
| 2 | Paper + benchmark para os itens pendentes do catálogo de métodos | Em andamento: +16 validados (53 → 69), com 9 equações por solução exata (`tests/test_manufactured_solutions_batch2.py`), condicionamento de telemetria e LBM |
| 3 | Abrir o visualizador 3D num navegador e ligar ao PINNeAPPle-CFD (desgaste no tempo) | Visualizador validado no Chrome (claro, escuro, celular); corrigidas cores sRGB e layout estreito; `?step=`/`?field=`. Ponte OpenFOAM → cena (`pinneapple_twin3d.openfoam`), testada no caso real do CFD. A troca na rota `erosao_3d` do CFD fica para quando a outra sessão terminar o merge em andamento lá |
| 4 | Sincronizar o checkout local com o `main` | Feito (sem tocar nas mudanças da outra sessão) |
| 5 | Este registro com a publicação marcada como feita | Feito |
| 6 | Commit dos docs do PINNeAPPle-CFD (PLAID, PressNet) | Feito localmente (`435ad37`); push pendente porque o `main` local do CFD tem 2 commits de outra sessão ainda não publicados |
| 7 | Modo autoresearch no `pinneapple_arena` | Feito: `pinneapple_arena.autoresearch`; rodada real: rel-L2 0,0115 → 0,00122 em 8 tentativas |
| 8 | Condicionamento de telemetria em tempo real no `digital_twin` | Feito: `pinneapple_systems.digital_twin.conditioning` (Hampel causal de um passo, limite de taxa, filtro de 1ª ordem com dt real, lacunas, perfis por tipo de sinal) |
| 9 | Exportação OpenUSD no `pinneapple_twin3d` | Feito: `scene.usda` com campos em timeSamples, validado com o `usd-core` da Pixar |
| 10 | Benchmark de LBM: esteira de Kármán no NACA 4412 (Strouhal) | Cilindro Re=100: St 0,1915 com bloqueio 10% (+16%) e 0,1742 com 5% (+5,8% vs Williamson 0,1647): validado. Nova `naca4_mask` com arqueamento (a antiga ignora os dígitos de arqueamento e inverte o sinal do α; documentado, não alterada por compatibilidade). Rodada do NACA 4412 pendente |
| 11 | Geometria do twin a partir de escaneamento (LiteReality) | Feito: `pinneapple_twin3d.scan` (limpeza + piso em +Z + unidades; importação de cena MuJoCo) |
| 12 | Aplicar a correção (caminho C) no notebook do Colab | Feito: `notebooks/PINNeAPPle_Climate_Digital_Twin_OpenMeteo.ipynb`, executado de ponta a ponta com `pinneapple` do PyPI. Achado no caminho: `import pinneapple` forçava o backend Agg e escondia os gráficos no Jupyter/Colab; corrigido |
| 13 | CalculiX (implícito) como o OpenRadioss é para explícito, na mecânica estrutural além do FEniCS; ideia do usuário: pôr o PINN no lugar do código FEM dentro do solver | Em andamento |
| 14 | Incluir PosteriorBench (neuraloperator) | Em andamento |
| 15 | Post "embedding one million 3D models" (finalrev.com) | Em andamento |
| 16 | Link LinkedIn `lnkd.in/p/dnBvzaKB` | Em andamento |
| 17 | Verificar o conceito de Large Physics Models e implementar o possível | Em andamento |
| 18 | Exemplos avançados de CadQuery para geometrias complexas: métodos/features, exemplos e reproduções no PINNeAPPle | Em andamento |

Notas dos links novos:
- **PosteriorBench** (neuraloperator, MIT, NeurIPS 2026, arXiv:2609.20794): benchmark de solvers generativos para problemas inversos (Poisson, Darcy, transporte de luz, CCS) que compara a distribuição a posteriori inteira, não só o ponto estimado. Métricas: MMD ponderada, Wasserstein fatiada com projeções GRF, espectro de potência radial.
- **finalrev.com, "Embedding one million 3D models"**: busca de CAD sem treinar modelo 3D: renderiza a peça, gera legenda com um VLM e indexa o embedding de texto (base ABC, 1M peças).
- **Ben Chang (LinkedIn)**: surrogate de crash com OpenRadioss + PhysicsNeMo-Curator + GeoTransolver, 135 simulações de DoE, deslocamento, von Mises e deformação plástica numa só passada. Lição registrada: treinar os canais juntos **sem normalização por canal** degrada o deslocamento quanto mais se treina.

## 2. Links e insights — onde cada um está

"Já registrado" = já estava documentado numa sessão anterior (conferido hoje).

| Link / insight | O que dá para usar | Onde está registrado | Ação |
|---|---|---|---|
| L1 CrunchOptimizer/PINNs (MIT) | Otimizadores quasi-Newton SSBFGS/SSBroyden. Benchmarks: Euler/HLLC, Helmholtz, Burgers invíscida, Stokes, PK-PD rígido | Já registrado: `ROADMAP.md` §"CrunchOptimizer/PINNs" | **Implementar** (pedido 5) |
| L2 karpathy/autoresearch (MIT) | Laço autônomo: agente edita um único `train.py`, orçamento fixo de tempo, uma métrica | Novo → `ROADMAP.md` §11 | Registrar; encaixa no `pinneapple_arena` |
| L3 nTop + CoreWeave (NASA 2030) | Geometria SDF + LBM sem malha ajustada ao corpo: 12 mil LES com zero falha de geometria | Já registrado: `ROADMAP.md`, `PINNeAPPle-CFD/docs/3.Referencias.md` | — |
| L4 LATTICEPT / Reactor Lab | LBM-LES + IBM na GPU pelo navegador; correlações de Gnielinski e Kawase-Moo | Já registrado: `ROADMAP.md` §7 | Referência de runtime para o visualizador 3D |
| L5 YouTube: esteira de Kármán, NACA 4412 (Sarwesh Parbat) | Caso clássico de desprendimento de vórtices (número de Strouhal) | Novo → `ROADMAP.md` §11 | Candidato a benchmark de LBM |
| L6 OpenV (MIT) | Verificação PASS/FAIL/UNKNOWN com invalidação de evidência; visualizador CAD em Three.js com cortes | Já registrado: `ROADMAP.md` §7 | Referência para o visualizador (cortes, isolamento de peça) |
| L7 Genesis World (Apache-2.0) | Bridge já existe em `external_solvers/genesis` | Já registrado: `ROADMAP.md` | — |
| L8 LiteReality-Agent (Apache-2.0) | Escaneamento → cena pronta para física, com agente que refina contra o scan | Novo → `ROADMAP.md` §11 | Registrar |
| L9 HamLeT (GPL-3) | Padrão: contrato de modelo, UQ por ensemble, checagem de compatibilidade dado↔modelo. Código GPL, não copiar | Novo → `ROADMAP.md` §11 | Registrar (só a ideia) |
| L10 Rayane Belbachir (post) | Condicionamento de telemetria em tempo real e visão SCADA de entrada × resposta | Novo → `ROADMAP.md` §11 | Encaixa em `digital_twin` e no visualizador (painel de sensores) |
| L11 Ian McGann (post) | Agente operando Simcenter AmeSim/PreScan de ponta a ponta | Novo → `ROADMAP.md` §11 | Registrar |
| L12 Sebastian Völkl / Dalus (post) | Diagrama → modelo consultável (MBSE) | Já registrado (Dalus no `ROADMAP.md`) | — |
| L13 Ashutosh Sharma / NVIDIA GTC (post) | OpenUSD como formato CAD → sim → twin, com proveniência | Novo → `ROADMAP.md` §11 | Formato de cena do visualizador: glTF agora, com OpenUSD no roadmap |
| L14 Mehul Bagaria (post) | Refino adaptativo de malha guiado por indicador de erro | Já registrado: `PINNeAPPle-CFD/docs/anexos/malha-automacao.md` | — |
| L15 luminary.ai/resources | Série SHIFT (modelos por domínio); posts técnicos dão 404 | Já registrado: `ROADMAP.md` §7 | — |
| L16 Wolfram: singularidade de Navier-Stokes | Só o fio de comentários abre; o notebook não | Novo → `ROADMAP.md` §11 | Nada a implementar |
| L17 Justin Hodges (747 no SolidWorks) | Nada técnico a implementar | — | — |
| L18 `ugcPost-7456788103509360641` | 404 (exige login) | — | Sem conteúdo |
| L19 Listas de datasets de geometria e de Physics AI | Avaliação completa com licenças | Já registrado: `PINNeAPPle-CFD/docs/7.Recursos-publicos.md` | Virar código (pedido 7) |
| L20 Mapa de modelos pré-treinados | Idem | Já registrado: mesmo doc, §6 | Virar código (pedido 8) |
| L21 PressNet | 150 simulações de estampagem (placa entre matrizes), 1500 passos, 3 malhas; baseline GNN. **Sem licença** no repositório | Novo → `7.Recursos-publicos.md` + `REFERENCIAS.md` | Catálogo, só referência |
| L22 PLAID competitions (HF) | 6 benchmarks com leaderboard: VKI-LS59, Rotor37, Tensile2d, 2D_profile, 2D_Multiscale_Hyperelasticity, 2D_ElastoPlastoDynamics. Dados em `PLAID-datasets` (CC BY-SA 4.0; AirfRANS em ODbL); lib `plaid` BSD-3 | Novo → `7.Recursos-publicos.md` + `REFERENCIAS.md` | Catálogo; **Rotor37 e VKI-LS59 são turbomáquinas** (casam com P4) |
| L23 AB-UPT | Pesos CC BY-NC; Noether ENPL | Já registrado + bridge só-pesquisa em `_licencas.py` | Catálogo |
| L24 GeoPT | Pesos e dados de downstream MIT; código sem licença; pré-treino em ShapeNet (NC) | Já registrado: `7.Recursos-publicos.md` §7 | Catálogo |

### Insights estratégicos dos posts (não são links, mas não podem se perder)

- **I1 — "Physics AI precisa de uma camada de geometria."** Ligar CAD, B-Rep, malha, condições de contorno, solver, campos e critério de validação num pipeline reutilizável. Na lib, isso é `pinneapple_design.geometry` (CAD/SDF/malha) + `pinneapple_catalog.resources` (datasets de geometria com licença) + o visualizador 3D (D1), que consome a mesma cena.
- **I2 — "Physics AI precisa de infraestrutura para descobrir, conectar, executar, validar e comparar milhares de modelos."** É a tese do `docs/dev/ROADMAP_PHYSICS_AI_HUB.md` ("o Hugging Face da Physics AI"). Hoje foi entregue a parte de "descobrir" (catálogo com licença e paper) e de "validar" (estado de validação por método). Executar e comparar são papel do `pinneapple_arena` e do `pinneapple_tools.benchmark_suite`.
- **I3 — pedido reenviado pelo usuário no meio da sessão** para garantir que nada se perdesse. Conferência feita item a item contra esta tabela: tudo coberto.
- **I4 — a lista de features começava com "- Visualização 3D…" e tinha um segundo item em branco ("- ").** Pode ter ficado incompleto: **perguntar ao usuário** qual era a segunda feature.

## 3. Decisões tomadas a partir da estrutura do `pinneapple-labs`

**D1 — Onde fica o visualizador 3D: `PINNeAPPle/pinneapple_twin3d`.**
A estrutura do ecossistema é: a capacidade reutilizável mora na lib `PINNeAPPle` (pacotes `pinneapple_*`), e os produtos (`PINNeAPPle-CFD`, `PINNeAPPle-apps`, `pinneapple_app`) só embrulham. O primeiro consumidor é o PINNeAPPle-CFD, cuja v1 exige visualização 3D do desgaste no tempo, mas `poc_predictive_twin` e `pinneapple_systems.digital_twin` também precisam. Por isso o visualizador vai para a lib:
- lado Python: monta a cena (malhas, campos, passos de tempo, sensores) e exporta glTF binário + manifesto;
- lado web: um visualizador estático, empacotado junto, que qualquer frontend embute (rota ou iframe).
Nenhum frontend atual tem three.js, então não há nada a reaproveitar nem conflito de versão.

**D2 — Onde registrar as referências.**
- Links que afetam o PINNeAPPle-CFD (datasets e modelos): `PINNeAPPle-CFD/REFERENCIAS.md` + `docs/7.Recursos-publicos.md`, seguindo a regra do repo.
- Links novos da lib: `PINNeAPPle/ROADMAP.md`, numa seção nova (§11), que é onde a lib já registra referências externas (§7, §9).
- Referências de cada método: no código (catálogo de métodos + docstrings), porque é assim que `pinneapple_pdb/benchmarks.py` já faz.

**D3 — Licenças não comerciais: entram no catálogo como "só pesquisa".**
É o padrão que já existe (`pinneapple_neural/_licencas.py`): aviso em uso normal e bloqueio com `PINNEAPPLE_COMMERCIAL_MODE=1`. Estende a mesma regra do `7.Recursos-publicos.md` §9 ("só benchmark interno"). Licença não declarada conta como não comercial até prova em contrário.

**D4 — Escopo do catálogo.**
Metadados de todos os itens (licença, uso comercial, paper, acesso, tamanho, classificação), com a mesma classificação do `7.Recursos-publicos.md`. Download real (`fetch`) para os itens hospedados no Hugging Face, sempre passando pela checagem de licença. Leitores dedicados de formato (AirfRANS, PDEBench, The Well, PLAID) seguem a ordem do plano já aprovado no §8 daquele doc.

**D5 — O que conta como "validado".**
Um método está **validado** quando existe teste em `tests/` que compara o resultado com uma referência independente: solução analítica, valor publicado ou solução manufaturada. O catálogo marca o resto como **"pendente: paper + benchmark"**.
**Não apago código.** Excluir mais da metade da lib por um critério automático seria destrutivo e difícil de desfazer. O item pendente sai da lista oficial de validados e só entra quando ganhar paper e benchmark, como você pediu.
